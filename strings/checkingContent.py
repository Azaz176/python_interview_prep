text = "Python3"
is_digit = text.isdigit()    # False (checks if all characters are digits)
is_alpha = text.isalpha()    # False (checks if all characters are alphabets)
is_alnum = text.isalnum()    # True  (checks if all characters are alphanumeric)
is_space = "   ".isspace()   # True  (checks if all characters are whitespace)
